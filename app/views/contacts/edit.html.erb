<h2>Editing <%= @contact.full_name -%></h2>

<% form_for @contact do |f| %>
  <%= f.error_messages %>
  <dl>
    <%= render :partial => 'form' -%>
    <dd><%= f.submit "แก้ไข", :class => "button" -%>&nbsp;&nbsp;<%= link_to 'ยกเลิก', @contact %></dd>
  </dl>
<% end -%>
